<Window x:Class="WIC_SDK_Sample.View.ProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ProgressWindow" Height="100" Width="300"
        WindowStyle="None"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <TextBlock Name="InfoLabel"  FontWeight="SemiBold" Margin="10,25,10,10" Text="Laden..." Grid.Row="0" VerticalAlignment="Center" HorizontalAlignment="Left" Grid.Column="1"/>

        <ProgressBar Name="ProgressBar" Minimum="0" Margin="10" Maximum="100" Width="270" Value="75" Grid.Row="1" Height="15" IsIndeterminate="True"/>
    </Grid>
</Window>
